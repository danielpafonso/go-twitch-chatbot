name: Unit Tests

on:
  push:
    branches:
      - main
  workflow_dispatch:

  jobs:
    unit-tests:
      runs-on: ubuntu-latest
      steps:
        - name: Get repo
          uses: actions/checkout@v4

        - name: Run Tests
          run: make test
